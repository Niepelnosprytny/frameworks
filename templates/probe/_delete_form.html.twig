<form method="post" action="{{ path('app_probe_delete', {'id': probe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ probe.id) }}">
    <button class="btn">Delete</button>
</form>
